============
Installation
============

At the command line::

    $ easy_install wasabiplot

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wasabiplot
    $ pip install wasabiplot
